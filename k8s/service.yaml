apiVersion: v1
kind: Service
metadata:
  name: wisecow
  labels:
    app: wisecow
spec:
  type: LoadBalancer
  selector:
    app: wisecow
  ports:
  - port: 80
    targetPort: 4499
    protocol: TCP
    name: http
