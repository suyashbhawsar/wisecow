apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wisecow
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
  labels:
    app: wisecow
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - wisecow.suyashbhawsar.com
    secretName: wisecow-tls
  rules:
  - host: wisecow.suyashbhawsar.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wisecow
            port:
              number: 80
